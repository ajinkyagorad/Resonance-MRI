using UnityEngine;

namespace Nebulytic.Resonance
{
    /// <summary>Marks a strip icon collider with its action (prev, play, next, voice, mode, credits).</summary>
    public sealed class StripButton : MonoBehaviour { public string Action; }
}
