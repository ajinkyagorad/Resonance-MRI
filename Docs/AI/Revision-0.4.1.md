# 0.4.1 feedback and regression record

This revision keeps the earlier 0.4.0 requirements and addresses the subsequent field, timing, attention, receiver, chronology and control reports. Baseline source: 03f062a494c165fc34fab05b5f61bdaf32f06353. Unity 6000.5.5f1, Android ARM64 IL2CPP and Windows x64, built on the headless server. No local browser or Unity instance was used.

| Report | Implemented behavior / evidence |
| --- | --- |
| Dark green field | Dedicated single-pass luminous field shader with leaf-green edges and pale core. Field strength modulates a bright palette; field visibility is independent of room illumination. Gradient contrast is visually amplified. |
| Cube and field coordinates disagree | Magnified tissue, moments and B0 share the inspection cube's coordinate map. Turning the cube turns the entire close-up; orbiting the camera leaves the physical scanner field unchanged. Runtime regressions cover both. |
| One RF cycle and unclear B1 | Six slowed cycles, three cycles per RF-current trace, orthogonal B1 components in the laboratory axes, and fifteen coherent B1 samples through the bore. The current pattern of the birdcage is checked against the central B1 direction. B1 denotes the transverse RF magnetic field. The tiny accumulated flip uses physical B1/B0. |
| Random coil glow | Transmit/gradient brightness follows instantaneous current. Green receive-coil emphasis indicates an active receiver; the arbitrary traveling shader band is removed. Cue selection uses a modest separate emphasis. Slice/encoding intervals replay with their narration and are labelled as replays. |
| Parameters reset narration | Removed the forced lesson selection and unconditional audio restart after asynchronous preparation. Regression failed before the fix, then passed for paused and playing narration. BW/current remain independent. B0/matrix move to RESULT; the slice page has only the two relevant controls. |
| Music-note mute control | Geometric speaker/wave and crossed-speaker icons, independent of font glyph availability. Actual action, mute state, hover help and theme contrast remain connected. |
| Missed plots / frozen nine markers | Receiver is beside the microscope, with its 3D trajectory directly below. The XY packets have an adjacent current waveform and time cursor. The final Y-encoding phrase replays the active interval. Nine markers are explicitly representative tissue packets; the dense ensemble in the scanned slice remains active. |
| ADC and FPGA missing or wrong focus | The enlarged receiver contains an ADC block and an FPGA enclosure with quadrature NCO/mixers and CIC/decimation. Phrase focus targets those actual blocks. A whole-slice voltage and selected/rest decomposition arrive before them. Hardware labels describe the documented MaRCoS architecture, not a universal receiver. |
| K-space before receiver explanation | Guided chronology is tissue phase, raw received RF, receiver I/Q, then k-space. K-space is hidden through lesson 9 and appears in lesson 10. I/Q labels and traces are hidden before their receiver lesson. Continuous scan retains causal sample acquisition. |
| What is scanned per row / slice | Every readout combines the whole excited slice. Changing Gy changes its spatial phase pattern and acquires another k-space row. The current row is indicated in the relief; RF/Gz/Gy/Gx/ADC and the tissue ensemble use the same sequence state. Moving the RF centre frequency under Gz selects subsequent Z slices. |
| Keep previous requests | Both MR/VR modes, desktop controls, twelve steps and transport, two male voices, voice-position preservation, one progress bar, light/dark switch, movable tissue/instruments, anatomy bones/muscles, coil isolation, slice controls, 3D I/Q and k-space relief, reconstruction, controller models/help and source notices are retained. |

Scientific references and the hardware/model distinction remain in Receiver-reference.md. A magnified inspection object is a coordinate-transformed view of tissue, rather than a new physical magnet. RF carrier and pulse-envelope clocks are expanded separately for legibility. The app computes idealized Bloch excitation and phase-weighted slice signals; it is an educational model.

Validation artifacts record exact runtime and APK checks, rendered views, build errors/warnings and signatures. All runtime evidence comes from the server. Physical Quest visibility, comfort, sustained frame rate, controller alignment, hand tracking, haptics and contextual boundary response remain untested. Windows is built and structurally checked on the server. Horizon preparation remains for a new app: actual Meta App ID, account fields, device entitlement/VRC validation and submission are outstanding.
