# Resonance MRI 0.5.0 feedback implementation

- Shared packet identities: orange A at (0, -12 mm), blue B at (0, +12 mm), and the same nine XY sites in the hand slab, tissue magnification and enlarged encoding view. X/Y/Z axis colours retain their separate meaning.
- A 60 mm crop of the actual segmented anatomy replaces the white density haze. A cutaway exposes tissue/fat/bone, 100 representative moments occupy three dimensions, and the selected slab responds to RF bandwidth and Gz current. Spatial links survive movement and rotation.
- Bright volumetric B0 lines cover the centre and sides of the bore and crop. Shader markers indicate +Z, with steady B0 explicitly identified. Forty-five orange B1 field samples rotate coherently through the bore. Coil shader motion integrates signed current and pauses with the sequence.
- New movable Slice Lab acquires one row or a single complete slice, compares equal-X/different-Y sources, scans a hand slice and accepts drawings/PNG density phantoms. Source, 3D receiver trace, acquired k-space relief, sequence and reconstructed image stay together.
- A selected source contributes to every k-space sample. Follow Pixel displays its incomplete-data point response; export includes the actual acquisition mask and I/Q data. Independent direct-sum and inverse-sum checks validate the discrete model.
- Teaching text and both male narrations explain whole-slice encoding, physical coordinates versus spatial frequency, and inverse phase matching. K-space remains downstream of the receiver explanation.
- A docked MaRCoS hardware reference identifies transmit, receive, ADC, FPGA mixing/filtering, gradient drive and reconstruction. A sourced supported-scanner concept distinguishes engineering proposals from implemented simulation.

Existing MR/VR contextual-boundary handling, animated official Touch Plus controllers, optional help, light/dark mode, movable objects, narration controls and 3D volume reconstruction are retained.

Validation results are generated by the release pipeline. Server render/build evidence does not certify headset performance, physical alignment or Store VRC compliance. The new Meta App ID and device validation remain required for final Horizon submission.
